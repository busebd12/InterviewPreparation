#include <iostream>
#include <cstdlib>
#include <algorithm>
#include <vector>
using namespace std;

int solution(const vector<long long int> & Numbers, long long int N, long long int Sum)
{
	if(N==1)
	{
		for(auto & number : Numbers)
		{

		}
	}
}

int main(int agrc, char* argv [])
{
	vector<long long int> testCase1={1, 2, 3};

	long long int sum {};

	cout << solution(testCase1, testCase1.size(), sum) << endl;

	/*
	vector<long long int> testCase2={1, 3, 6};

	cout << solution(testCase2, testCase2.size());
	*/
}

